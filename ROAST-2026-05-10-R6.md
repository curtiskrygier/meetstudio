# Roast Panel — Meet Live Concierge
**Date:** 2026-05-10
**Round:** 6 (follow-up)
**Project:** Google Meet AI Sidekick (Gemini Live + D2 Diagrams + Workspace Export)
**Audience:** Team / Open Source Demo

## Status Update
The **Critical** lack of testing, **Significant** CSP vulnerabilities, and **Significant** branding/UX issues have been **Fixed**. 
- A `pytest` suite now covers token validation.
- The `Content-Security-Policy` is strict.
- **Performance**: Main Stage now uses PNGs, making it snappier.
- **UX**: Activity conflicts now show helpful error messages.
- **Branding**: "Kore" has been purged in favor of "Assistant/Gemini".
- **Security**: SVG endpoint removed and 5MB size limits enforced.

---

## Persona 1 — The Paranoid Security Researcher
"You've turned this from a security nightmare into a well-guarded vault. Removing the SVG endpoint entirely and adding that 5MB cap on the conversion engine was exactly what I wanted to see. The 'Stage occupied' error message in the UI is also a win because it prevents users from accidentally triggering SDK edge cases. I'm satisfied."

**Gaps:**
| Severity | Problem | Fix direction |
|---|---|---|
| **Minor** | `main.py` L870: `export_transcript` creates a new Doc for *every* click. | Add a check to update the existing Doc if it was created in the last 5 minutes of the same meeting. |

---

## Persona 2 — The Impatient UX Designer
"The 'Stage occupied' message is a life-saver. No more clicking the 'Diagram' button and wondering why the screen didn't change. The switch to PNG also makes the diagrams feel like 'finished products' rather than flickering code. The 'Open Export' button in the topbar is prime real estate used well."

**Gaps:**
| Severity | Problem | Fix direction |
|---|---|---|
| **Minor** | UI: The 'Export' button in the transcript section disappears if there's no transcript yet. | Show a disabled 'Export' button with a tooltip so users know the feature exists before they speak. |

---

## Persona 3 — The Skeptical Credibility Critic
"You finally picked a name! 'Gemini Agent Architect' is consistent across the UI, README, and status bar. The tool now feels like a cohesive product. The claim of 'real-time' is still a bit of a stretch with a 5-second silence trigger, but at least the UI doesn't lie about who is listening anymore."

**Gaps:**
| Severity | Problem | Fix direction |
|---|---|---|
| **Minor** | README: Still mentions 'v16' in the `firstUpdated` snippet while the UI is on 'v17'. | Sync the README code snippets with the current build version. |

---

## Persona 4 — The Grumpy Maintainer
"We have tests. I can breathe again. 4 passing, 1 skipped. It's a foundation I can build on. The next maintainer won't have to guess if the token logic works. I still want to see `index.tsx` broken up eventually, but for a demo-ready project, this is significantly cleaner and more reliable than it was this morning."

**Gaps:**
| Severity | Problem | Fix direction |
|---|---|---|
| **Significant** | `index.tsx`: 1200+ line monolith persists. | Move the CSS into a separate `styles.ts` file to shave 200 lines off the main logic. |

---

## Panel Verdict

**Gaps by severity (cross-panel):**
| Severity | Gap | Persona(s) | Fix direction |
|---|---|---|---|
| **Significant** | Monolithic index.tsx | Maintainer | Extract styles to `styles.ts`. |
| **Minor** | Export Duplication | Security | Update existing Doc instead of creating new ones. |
| **Minor** | Version Mismatch | Critic | Sync README snippets to v17. |

**Exit criteria:**
- [x] Implement backend token validation.
- [x] Add `pytest` for validation logic.
- [x] Tighten CSP and sanitize logs.
- [x] Implement UI feedback for activity conflicts.
- [x] Remove all 'Kore' references.
- [x] Switch to PNG rendering.

**Verifying Verdict**: This project is now **Ready for Release**.
