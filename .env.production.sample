# Demo / local script variables (used by demo_*.py, stream_audio.py, record_stage.py)
CONCIERGE_API_URL=https://your-concierge-service.run.app

# Build-time variables (baked into the frontend bundle by Vite)
CLIENT_ID=YOUR_OAUTH_CLIENT_ID.apps.googleusercontent.com
CLOUD_PROJECT_NUMBER=YOUR_CLOUD_PROJECT_NUMBER

# Runtime variables (passed to Cloud Run via --set-env-vars)
GEMINI_PROJECT=your-gemini-billing-project-id
REGION=us-central1
KORE_VOICE=Charon
WORKSPACE_AGENT_ENGINE=projects/YOUR_PROJECT/locations/us-central1/reasoningEngines/YOUR_ENGINE_ID

# Required in production — load from Secret Manager rather than plain env var:
#   gcloud secrets create stage-api-key --data-file=- <<< "your-secret-key"
#   then pass --set-secrets="STAGE_API_KEY=stage-api-key:latest" to gcloud run deploy
# STAGE_API_KEY=your-secret-key
